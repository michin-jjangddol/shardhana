Update the existing 1D HEM simulation code to include a fracture (break) condition based on contact face reduction.

### Goal

Implement a simple fracture logic:

* When contact face drops below a threshold, the element is considered broken.
* Broken elements should stop interacting with neighbors.

---

### Requirements

#### 1. Seed Class Update

Add the following properties:

* `broken = False`
* `face_threshold = 0.2`

---

#### 2. Fracture Condition

In the simulation loop (e.g., `run_hem()` or `update_geometry()`):

* If `face_right < face_threshold` OR `face_left < face_threshold`:
  → set `broken = True`

---

#### 3. Interaction Rule Update

* If a Seed is `broken == True`:

  * Exclude it from neighbor interaction calculations
  * It should not influence adjacent seeds anymore

* Important:

  * Neighbor seeds should **not automatically break**
  * Their contact face should remain unchanged unless affected by normal logic

---

#### 4. Visualization Update

Update the plot to show fracture clearly:

* Plot contact face evolution over time
* Draw a horizontal line at `face_threshold = 0.2`
* Mark broken seeds using:

  * different color (e.g., red)
  * or different marker (e.g., "x")

Optional:

* Show the time step when fracture first occurs

---

### Constraints

* Do NOT add GUI input for `face_threshold` (keep it fixed at 0.2)
* Focus on behavior verification, not parameter tuning
* Keep code structure simple and readable

---

### Objective

The purpose is NOT accurate material modeling yet.

The purpose is to verify:
→ contact face reduction → fracture detection → interaction cutoff

---

Return the full updated code.

--- ORIGINAL CODE START ---
(여기에 기존 1D 코드 전체 붙이기)
--- ORIGINAL CODE END ---
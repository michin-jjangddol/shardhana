Update the current v3c code with minimal changes.

Goal:
Fix the issue where multiple interfaces weaken simultaneously.

---

Requirements:

1. Modify only the necessary lines.
2. Do NOT rewrite the full code.
3. For each change, show:

   - approximate location (e.g. function name or nearby code)
   - BEFORE code
   - AFTER code

---

Main fix:

Currently, both face_left and face_right use the same state_diff.

Change to directional logic:

- state_diff_left  = abs(seed[i].state - seed[i-1].state)
- state_diff_right = abs(seed[i].state - seed[i+1].state)

- face_left  = f(state_diff_left)
- face_right = f(state_diff_right)

---

Goal:

Only the interface with the largest local difference should weaken first.

Keep everything else unchanged.
We are updating Shardhana 1D HEM prototype (necking_1d_gui.py, v3c → v3d).

Goal:
Add CSV logging for verification.

IMPORTANT:
Do NOT rewrite the full code.
Show ONLY the minimal changes needed.
Explain where to insert or modify.

---

Requirements:

1. Add CSV logging with these fields:
   - step
   - state
   - volume
   - interface_k
   - broken (0 or 1)

2. Implementation style:
   - create a list to store log data (before simulation loop)
   - append data at each step
   - write to CSV AFTER simulation ends

3. Output:
   - file name: simulation_log.csv
   - must open in Excel

---

Response format:

1. Where to add code (e.g., "before main loop", "inside loop", "after loop")
2. Only the code snippets to add or modify
3. Very short explanation (1–2 lines max)

---

Do NOT return full file.
Only show incremental patch-style changes.

Goal:
Help user understand the code step-by-step, not just copy-paste.

---

[PASTE YOUR v3c CODE BELOW]


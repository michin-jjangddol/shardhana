We are updating Shardhana 1D HEM prototype (necking_1d_gui.py, v3d).

Goal:
Prevent CSV overwrite by saving each run as a unique file.

IMPORTANT:
Do NOT rewrite the full code.
Show ONLY minimal changes (patch-style).

---

Current behavior:
- CSV is saved as "simulation_log.csv"
- It gets overwritten every run

---

Required change:

1. Modify ONLY the CSV filename part:
   - Add timestamp to filename
   - Example:
     simulation_log_20260505_101530.csv

2. Implementation hint:
   - Use datetime
   - Format: "%Y%m%d_%H%M%S"

3. Keep everything else exactly the same:
   - same logging structure
   - same CSV fields
   - same save location (script directory)

---

Response format:

1. Where to modify (exact location)
2. Code snippet BEFORE
3. Code snippet AFTER
4. Very short explanation (1 line)

---

Do NOT return full file.
Only show the minimal change.